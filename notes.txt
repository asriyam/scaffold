The swagger-import component, loads the swagger json. I use the "Extract" link in desired api method item, to open the "Extract AI Response" flyout. In the flyout, I can see the query string parameters are shown in the "Sample Request (JSON)" but not showing the requestbody. This is wrong. Each querystring parameter should be shown as separate textbox input and the requestbody should be loaded in the "Sample Request (JSON)" box. When the user uses "Send Request" button, the request parameters should be updated in the API url and the requestbody json posted to the API.  Analyze and plan this change. 


==
Functional
1. Try It Out - Add button "Show Request" - so user can edit the request. 
2. Flow input to be added in the requestData
3. inputMapping to be added in the orchestration
4. Default inputs - skipCache, includeMetrics, refresh


UI
1. Don't show parallel
2. Remove the words "Mapping"
3. Remove the field types, Body
4. Make it accordion for Inputs and Field Selection
5. Remove toggle and use checkbox before
6. Add advanced tab only when "debug=true"

===
